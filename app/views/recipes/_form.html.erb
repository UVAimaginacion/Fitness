<%= form_for @recipe, html: { multipart: true } do |f| %>
  <%= render 'shared_recipes/error_messages' %>

    <div class="form-group">
      <%= f.label :name, 'Nombre' %>
      <%= f.text_area :name, autofocus: true, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= f.label :ingredient, 'Ingredientes' %><br>
      <%= f.text_area :ingredient, autofocus: true, class: "form-control", size: "10x10" %>
    </div>

    <div class="form-group">
      <%= f.label :description, 'Descripción' %><br>
      <%= f.text_area :description, autofocus: true, class: "form-control", size: "20x20" %>
    </div>

    <div class="form-group">
      <p>
        <%= f.label :image, 'Imagen de la Receta' %>
      </p>
      <p>
        <%= image_tag @recipe.image.url(:medium) %>
      </p>
      <p>
        <%= f.file_field :image %>
      </p>
    </div>

    <div class="form-group">
      <%= f.submit 'Guardar', class: "btn btn-primary" %>
    </div>
<% end %>


